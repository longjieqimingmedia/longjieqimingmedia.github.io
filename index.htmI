<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>微表情识人术 - 瞬间捕捉情绪的秘密</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        /* 全局样式 */
        body {
            font-family: "PingFang SC", "Microsoft Yahei", sans-serif;
            background-color: #1E80FF; /* 整页纯蓝色背景 */
            color: white;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }
        /* 封面区域（纯蓝色，无任何多余元素） */
        .cover {
            width: 100%;
            max-width: 700px;
            background-color: #1E80FF; /* 纯蓝色封面 */
            color: white;
            padding: 4rem 2rem;
            text-align: center;
            margin-bottom: 2rem;
        }
        .book-title {
            font-size: 3.8rem;
            font-weight: bold;
            margin-bottom: 1rem;
            text-shadow: 0 3px 10px rgba(0,0,0,0.2);
        }
        .book-subtitle {
            font-size: 1.6rem;
            opacity: 0.9;
        }
        /* 内容介绍区域（半透明白色背景） */
        .content {
            background: rgba(255,255,255,0.15);
            padding: 2.5rem;
            border-radius: 16px;
            backdrop-filter: blur(8px);
            max-width: 700px;
            margin-bottom: 2rem;
        }
        .content p {
            margin: 1.2rem 0;
            line-height: 1.8;
            font-size: 1.15rem;
        }
        /* 收款二维码区域（独立模块，不影响封面） */
        .qrcode-box {
            background: rgba(255,255,255,0.95);
            padding: 2rem;
            border-radius: 16px;
            box-shadow: 0 6px 20px rgba(0,0,0,0.15);
            text-align: center;
            max-width: 300px;
            margin-bottom: 2rem;
        }
        .qrcode-box img {
            width: 220px;
            height: 340px;
            border-radius: 12px;
            border: 3px solid #1E80FF;
        }
        .qrcode-tip {
            font-size: 1.1rem;
            margin-top: 0.8rem;
            color: #333;
        }
        /* 页脚（底部已留白，不贴边） */
        .footer {
            margin-top: auto; /* 自动推到页面底部 */
            margin-bottom: 2rem; /* 底部留白，避免贴边 */
            font-size: 0.95rem;
            opacity: 0.7;
            text-align: center;
        }
    </style>
</head>
<body>
    <!-- 纯蓝色封面 -->
    <div class="cover">
        <div class="book-title">微表情识人术</div>
        <div class="book-subtitle">瞬间捕捉情绪的秘密</div>
    </div>

    <!-- 电子书内容介绍 -->
    <div class="content">
        <p>📖 本书教你通过细微表情，读懂他人真实情绪</p >
        <p>💡 掌握职场、社交、亲密关系中的读心技巧</p >
        <p>🎯 让你在人际交往中更通透、更有主动权</p >
        <p>✨ 从眼神、嘴角、眉梢，拆解情绪密码</p >
    </div>

    <!-- 收款二维码（独立模块，不放在封面上） -->
    <div class="qrcode-box">
        < img src="qrcode.png" alt="农行收款二维码">
        <div class="qrcode-tip">📱 扫码付款，立即获取全书</div>
    </div>

    <!-- 页脚（底部已留白） -->
    <div class="footer">© 2026 河北龙杰启明传媒 出品</div>
</body>
</html>
